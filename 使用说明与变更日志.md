# 光遇 PC MCP 服务器 · 使用说明与变更日志

> 更新时间：2026-08-26
> 主程序文件：sky-mcp-server.py

---

## 〇、新人安装完整步骤（第一次用请看这里）

**要准备的东西：** Windows 10/11 电脑 + 能正常运行的 PC 版光遇。

1. **装 Python 3.11**（推荐版本，本项目脚本按 3.11 配置）
   - 下载：<https://www.python.org/downloads/release/python-3119/>
   - 安装时**务必勾选 “Add python.exe to PATH”**
2. **下载代码**：打开 <https://github.com/sereinliovo-byte/sky-pc-mcp-companion> → 点绿色 `Code` → `Download ZIP` → 解压到任意英文路径文件夹
3. **装核心依赖**：进入代码文件夹，空白处右键 →“在终端中打开”→ 运行：
   ```
   python -m pip install -r requirements.txt
   ```
4. **装识图工具**
   - 推荐（中文识别好）：`python -m pip install paddleocr paddlepaddle`，首次运行会自动下载识别模型（要联网）
   - 轻量替代：先安装 Tesseract 软件（<https://github.com/UB-Mannheim/tesseract/wiki>），再运行 `python -m pip install pytesseract`
5. **启动**：双击 `start-http.bat`，或参考下面「一、怎么启动服务器」
6. **连接**：地址 `http://127.0.0.1:9800`（手机用局域网 IP），Token `（自定义）`

---
## 一、怎么启动服务器

- 最简单：双击桌面的「Sky MCP 服务器」快捷方式（以管理员身份运行，会弹蓝色确认框，点「是」）
- 会弹出一个黑色控制台窗口，里面有地址和 token，**关掉这个窗口 = 停止服务器**
- 服务器信息：
  - 地址：http://0.0.0.0:9800
  - Token：（自定义）
  - 手机客户端连接：http://这台电脑的局域网IP:9800 ，认证填 Bearer （自定义）

如果不用快捷方式，也可以直接命令行启动：

```
python sky-mcp-server.py --http --host 0.0.0.0 --port 9800 --token （自定义）
```

常见启动参数（都有默认值，平时不用管）：

| 参数 | 默认值 | 说明 |
|---|---|---|
| --host | 127.0.0.1 | 0.0.0.0 才能让手机/局域网连进来 |
| --port | 9800 | 端口 |
| --token | 环境变量 SKY_MCP_TOKEN | 连接密钥 |
| --window-title | 环境变量 SKY_WINDOW_TITLE | 找不到光遇窗口时手动指定窗口标题 |
| --monitor | 1 | 截图用的显示器编号 |
| --input-backend | auto | auto / pyautogui / pydirectinput |
| --screenshot-max-width | 1920 | 截图最大宽度 |
| --screenshot-max-height | 1080 | 截图最大高度 |

改完代码后**必须重启服务器**才生效（关旧窗口 → 重新双击快捷方式）。

---

## 二、已完成的功能（变更日志）

### 第一阶段：读屏优化
1. **OCR 结果缓存 + 新增内容检测**
   - 每次读屏会保存上一次的识别结果，下次只对比「多了什么」
   - 屏幕没变化时返回：No new content
2. **噪音过滤**
   - 过滤掉：光遇标题、陌生人/玩家名堆、蜡烛数、时间、纯数字、破折号碎片、**** 屏蔽字、按钮词（聊天/发送/设置/好友等）
   - 过滤规则：置信度 < 0.8 的行、太短的行、纯符号行
3. **位置感知比对**
   - 同样一句话换了位置不重复上报（判断标准：文字相似度 ≥ 90% 才算同一句；2026-08-23 收紧，之前「位置近 + 相似度 ≥40%」在快聊时容易误吞）
4. **已返回临时库（现在是 5 分钟）**
   - 已经返回过的文字，5 分钟内不再返回给 AI
5. **新工具：wait_for_screen_change（等待读屏）**
   - 每隔 1 秒自动读一次屏，把连续读到两次的新内容攒起来，等最新内容稳定后一起返回
   - 超时也不丢内容，会把已确认的部分返回（带 confirmed=false 标记）
6. **read_screen 只返回新增的干净文字**
   - 有变化时返回 texts（新增行）+ text（纯文本），不再把整屏原文塞给 AI
7. **exe 一键启动器 + 桌面快捷方式**
   - Sky-MCP-Server.exe 是启动器，双击即可启动（管理员模式）

### 第二阶段：发送与对话优化
8. **发送确认（重要）**
   - AI 调用一次发送工具，服务器内部完成：发送 → 等 1 秒 → OCR 读屏 → 没读到立刻再读一次 → 返回「发送成功 / 发送失败」
   - AI 不需要自己再去读屏确认，也不会把自己发的话当成新消息
9. **短回复放行**
   - 单个中文字符（好/嗯/哦/啊…）只要置信度 ≥ 0.9 就保留返回；单数字、字母、符号仍过滤
10. **长消息自动分段**
    - 按句号/感叹号/问号/破折号（。！？!? 和 ——）切分，省略号（…）不拆
    - 问号？感叹号！保留在句尾（语气不丢）；句号和破折号不保留
    - 有 2 句以上就拆成多条，每条间隔 0.5 秒发送，最后统一确认一次
11. **临时库 90 秒 → 300 秒（5 分钟）**
    - 已返回/已发送的内容 5 分钟内不再重复返回

### 第三阶段：坐标置信度框架（字高比例尺，2026-08-20）
12. **字高尺子**
    - OCR 结果新增每行文字的「高度」，当作比例尺
    - 所有坐标判断都用「字高的倍数」来算，游戏视角放大缩小都不受影响
13. **聊天列识别**
    - 把横向位置相近的文字归成同一「列」（容差 ±3.5 倍字高）
    - 连续 3 帧在几乎同一位置出现的短文字 = 昵称锚点（玩家所在的 X 轴）
    - 聊天列确认（满足任一即可）：① 同一列 2 条以上、纵向间隔 0.8~3 倍字高；② 稳定昵称 + 同列出现气泡
14. **单字规则升级**
    - 聊天列内的单字（好/嗯/F…）→ 保留；列外单字仍按「中文字 + 置信度 ≥0.9」
15. **F 互动检测（当前已关闭）**
    - 原因：游戏里好友靠近也会出现 F，误报太频繁，会让「等待读屏」失效
    - 已默认关闭（_F_DETECT_ENABLED = False），代码保留，等想好更准的判定标准再开启
    - 同时单字 F 也不再作为聊天内容返回
16. **发送确认改成「只读模式」（2026-08-22）**
    - 发送后的确认读屏不再更新屏幕记忆：确认期间如果屏幕上有别人的新消息，不会被当成「已见过」吞掉
    - 你自己发的话如果在确认期间上屏，之后 AI 读屏仍能正常识别到
    - AI 自己发送的内容仍然照旧进临时库，不会被当成新消息重复返回
17. **读屏顺序 + 已发送识别 + 相似去重收紧（2026-08-23）**
    - 读屏返回顺序：read_screen 和 wait_for_screen_change 返回前会按屏幕位置从上到下排序（新消息在下面，从上到下就是说话顺序）
    - 已发送识别升级：发送时会额外记录「去掉空格和标点后的句子」；60 秒内读屏只要读到这个句子的任意片段（≥2 个字），都当作已发送内容不返回，解决「AI 自己发的话被拆成片段返回」的问题
    - 相似去重收紧：两行文字相似度 ≥ 90% 才算同一句；原来的「位置很近（90×70 像素内）+ 相似度 ≥40% 就算重复」规则已去掉，解决快聊时「好的/好呀」「在吗/在吗在吗」这类相似句子被误吞的问题
18. **长消息两行顺序反了 + 气泡被拆开（2026-08-23 晚，二次修正）**
    - 真实原因：OCR 返回结果时，把气泡内两行的「文字」和「位置框」配对错位了（识别顺序和检测顺序恰好不同），导致一行长消息被拆成两截、且上下位置互换
    - 已修复：OCR 结果先按「从上到下、从左到右」的位置重新排好，再和文字配对，长消息的两行不再错位
    - 返回顺序保持「从上到下」（新消息在下面，从上到下就是说话顺序）
    - 同一气泡内连续两行（换行）会自动拼成一条完整消息返回，不再拆成两截

### 第四阶段：星盘传送准备（调试工具，2026-08-23）
19. **click_at —— 点击屏幕坐标（调试用）**
    - 参数：x / y（屏幕绝对坐标，直接用 read_screen 返回的 screen_x / screen_y）、button（默认 left）、clicks（默认 1）
    - 点击前记得先 focus_game（让光遇在前台）
20. **read_screen 返回全部原始识别内容**
    - 新增 all_lines 字段：返回 OCR 读到的每一行（text / confidence / x / y / height），并额外给出屏幕绝对坐标 screen_x / screen_y
    - 原来的 texts / text（过滤后的新增内容）保持不变，聊天流程不受影响
    - 用途：星盘/好友页调试坐标 —— 先读屏拿坐标，再用 click_at 试点

21. **修复：AI 按键会「返回上一页」（抢焦点流程的锅）**
    - 原因：每次 press_key 都会先 focus_game 抢焦点，而 focus_game 会给游戏发一次 Alt 按下/抬起；游戏已在前台时也会发，可能干扰游戏操作；焦点没抢到时还会在窗口中心点一下鼠标，可能误触 UI 导致返回上一页
    - 修复①：游戏已在前台时 focus_game 直接跳过，不再发多余的 Alt/点击
    - 修复②：press_key 新增 assume_focused 参数，连续按键时设 true 可完全跳过聚焦步骤
    - 兜底开关：抢焦点失败的「点窗口中心」可以关掉（_FOCUS_FALLBACK_CLICK = False）

22. **新工具：press_keys —— 连续按键（支持最后一下长按）**
    - 用法：press_keys(keys="fff") = f 连按 3 次；带空格则按空格分词（"f f f"、"space space"）
    - 参数：interval_ms（间隔，默认 80）、duration_ms（普通按住时长，默认 60）、last_duration_ms（最后一下按住时长，默认 120）
    - 只有一下时按 last_duration_ms 算；设成和 duration_ms 一样就是每下都相同
    - 单次最多 20 下，防手滑；一次调用只聚焦一次（assume_focused 可跳过）

23. **新工具：teleport_to —— 星盘一键传送**
    - 用法：teleport_to(friend_name="A瑜卿")，一次调用完成：按 G 开星盘 → 读屏找名字 → 点名字右侧（x+100）→ 读屏找「传送」→ 点击 → 空格确认
    - 参数：open_key（开星盘键，默认 g）、name_offset_x/y（名字偏移，默认 +100, 0）、button_text（传送按钮文字，默认 传送）、confirm_key（确认键，默认 space）、open_delay_ms（默认 800）、confirm_delay_ms（默认 1000）、max_attempts（默认 3）、assume_focused、dry_run
24. **teleport_to：空格确认前等待改为 1 秒（2026-08-24）**
    - 实测发现点完「传送」后只等 400ms 就按空格，游戏没识别上
    - confirm_delay_ms 默认值 400 → 1000；想改就传 confirm_delay_ms=xxx
25. **teleport_to：确认空格连按两次（2026-08-24）**
    - 实测：确认要连按两次空格，两次之间间隔可以短
    - 新增 confirm_repeat（默认 2）、confirm_interval_ms（默认 120）
    - dry_run=true 只定位不点击，先用来调试偏移
    - 找不到名字/按钮会报错并给出已找到的坐标，方便排查

---

## 三、每个工具怎么调用（给 AI / 客户端的说明）

> 调用方式就是 MCP 标准的 tools/call：填工具名 + 参数。
> 下面「调用示例」用 工具名(参数=值) 的形式表示。

### 1. status —— 查看服务器状态
- 参数：无
- 返回：模式、输入后端、OCR 引擎、窗口检测结果等
- 调用示例：status()

### 2. focus_game —— 把光遇窗口切到前台
- 参数：无
- 返回：窗口信息 + 是否聚焦成功
- 调用示例：focus_game()

### 3. press_key —— 按键
- 参数：
  - key（必填）：键名，如 w/a/s/d、space（跳/飞）、f（互动）、q（呼唤）、tab（飞行模式）、enter、shift-space
  - duration_ms：按住时长，默认 80 毫秒
  - backend：输入后端，默认 auto
  - assume_focused：游戏已在前台时设 true，跳过重新聚焦（连续按键推荐），默认 false
- 调用示例：press_key(key="space", duration_ms=100)

### 4. open_chat —— 轻点聊天键（测试用）
- 参数：
  - key：聊天键，默认 enter
  - duration_ms：轻点时长，默认 35 毫秒
  - backend：默认 auto
- 调用示例：open_chat()

### 5. send_chat —— 打开聊天框并发送（最常用的发送工具）
- 参数：
  - message（必填）：要发的内容，支持中文和表情
  - open_key：打开聊天框的键，默认 enter
  - open_delay_ms：打开后等多久再粘贴，默认 180 毫秒
  - assume_open：聊天框已经打开时设 true（跳过开框），默认 false
  - send：设为 false 只粘贴不发送，默认 true
  - enter_tap_ms：回车轻点时长，默认 35 毫秒
  - backend：默认 auto
- 自动行为：长消息按句子自动分段（间隔 0.5 秒），发完后服务器自动确认
- 返回示例：
  - sent chat: 你好呀 / 今天怎么样 (2条) via pydirectinput
  - 发送成功
- 调用示例：send_chat(message="你好呀。今天怎么样？")

### 6. type_text —— 往已打开的聊天框粘贴并发送
- 参数：
  - message（必填）
  - send：默认 true
  - enter_tap_ms：默认 35
  - backend：默认 auto
  - require_foreground：要求光遇在前台，默认 true
- 适用：用户已经手动打开了聊天输入框
- 同样支持分段 + 发送确认
- 调用示例：type_text(message="来了！")

### 7. read_screen —— 读一次屏幕
- 参数：无
- 返回：所有 OCR 识别到的内容都在 all_lines 里（每行带 text / confidence / x / y / height，以及屏幕绝对坐标 screen_x / screen_y）；同时保留原来的 texts（新增的干净文字）+ text 字段
- 没变化时 text 仍是 No new content，但 all_lines 依然有全部内容
- 注意：这个是一次性读取，监控请用 wait_for_screen_change
- 调用示例：read_screen()

### 8. wait_for_screen_change —— 等待读屏（监控聊天推荐用这个）
- 参数：
  - timeout_seconds：等待秒数，默认 60，允许 1~600
- 行为：
  - 每 1 秒自动读一次屏
  - 每行内容连续读到两次才确认
  - 等最新内容稳定后，把攒住的所有新内容一起返回（text 字段）
  - 超时时：有已确认内容就返回（confirmed=false），没有就返回 No new content for Ns
- 调用示例：wait_for_screen_change(timeout_seconds=60)

### 9. take_screenshot —— 截图
- 参数：无
- 返回：一张 PNG 图片
- 调用示例：take_screenshot()

### 10. click_at —— 点击屏幕坐标（调试用）
- 参数：
  - x（必填）：屏幕绝对 X 坐标，用 read_screen 返回的 screen_x
  - y（必填）：屏幕绝对 Y 坐标，用 read_screen 返回的 screen_y
  - button：left / right / middle，默认 left
  - clicks：点击次数，默认 1
- 点击前记得先 focus_game（确保光遇在前台）
- 调用示例：click_at(x=960, y=540)

### 11. press_keys —— 连续按键（连按 fff）
- 参数：
  - keys（必填）：按键串。不带空格时每个字符按一次（"fff" = f 连按 3 次）；带空格时按空格分词（"f f f"、"space space"）
  - interval_ms：两次之间的间隔，默认 80 毫秒
  - duration_ms：每次按住时长（最后一下除外），默认 60 毫秒
  - last_duration_ms：最后一下按住时长，默认 120 毫秒（和 duration_ms 设成一样 = 每下都一样）
  - assume_focused：游戏已在前台时设 true，跳过重新聚焦，默认 false
- 单次最多 20 下；只有一下时按 last_duration_ms 算
- 调用示例：press_keys(keys="fff")；press_keys(keys="fff", interval_ms=120, last_duration_ms=200)

### 12. teleport_to —— 星盘一键传送
- 参数：
  - friend_name（必填）：好友名字
  - open_key：开星盘的键，默认 g
  - name_offset_x / name_offset_y：从名字位置偏移多少去点星星，默认 +100 / 0
  - button_text：传送按钮文字，默认 传送
  - confirm_key：确认键，默认 space
  - open_delay_ms：开星盘/点完星星后的等待，默认 800
  - confirm_delay_ms：点完传送后到按确认键的等待，默认 1000（游戏需要约 1 秒，太快空格会没识别上）
  - confirm_repeat：确认键按几次，默认 2（游戏要连按两次空格）
  - confirm_interval_ms：两次确认键之间的间隔，默认 120
  - max_attempts：OCR 重试次数，默认 3
  - dry_run：true = 只定位不点击（先用来调偏移）
- 返回每步的坐标报告；找不到名字/按钮会报错
- 调用示例：teleport_to(friend_name="A瑜卿")；teleport_to(friend_name="A瑜卿", dry_run=true)

---

## 四、内部逻辑细节（知道这些更好用）

### 发送确认的完整流程
1. 发送内容后，把这段文字记入临时缓冲
2. 等 1 秒 → OCR 读屏一次
3. 拿读到的每一行和缓冲里的文字比对：
   - 你的话出现在那一行里（如「宝宝：你好呀」包含「你好呀」）→ 算命中
   - 或相似度 ≥ 60% → 算命中
4. 第一次没命中 → 立即再读一次
5. 命中 → 返回「发送成功」；两次都没命中 → 返回「发送失败」
6. 命中后，那一行和发送内容都进临时库（5 分钟内不再返回）

> 确认读屏是「只读」的：只读屏、不做任何记忆更新。因此不会把你（用户）在确认期间发出的新消息当成「已见过」。

小规则：很短的回复（≤2 个字）匹配会更严格，要求屏幕上基本就是这句话，避免发「好」却被「你好」误判成功。

### 自动分段的规则
- 切分符号：。！？!? 和 ——（破折号）
- 分段后：问号？感叹号！保留在句尾；句号和破折号去掉
- 不切：省略号 …（会保留在内容里）
- 每条单独发，间隔 0.5 秒
- 单句超过 240 字才会强制硬切（兜底）

### 噪音过滤规则（读屏时）
- 置信度 < 0.8 的行直接丢弃
- 纯数字、时间、蜡烛数、光遇标题、陌生人名、按钮词等丢弃
- 单个中文字符（好/嗯/哦/啊）置信度 ≥ 0.9 保留
- 单个数字/字母/符号仍丢弃

### 等待读屏的确认规则
- 连续两次读到相同内容才算确认
- 内容一直在变时不会中途返回，攒到稳定后一起返回
- 超时也会返回已确认的部分（不丢内容）
- 返回顺序：按屏幕位置从上到下（新消息在下面，从上到下就是说话顺序）；同一气泡内换行的几行会自动拼成一条完整消息

### 坐标置信度（字高比例尺）
- 比例尺 = 每行文字的高度；所有判断用「字高倍数」，游戏视角缩放不影响
- 聊天列：横向中心相差 ±3.5 倍字高以内的文字归为一列
- 昵称锚点：连续 3 帧在几乎同一位置出现的 2~6 字短文字（取列内最上面那个）
- 聊天列确认：① 同列 2 条以上、纵向间隔 0.8~3 倍字高；② 稳定昵称 + 同列有气泡
- 单字：聊天列内的单字保留；列外单字需「中文字 + 置信度 ≥0.9」
- F 互动：已默认关闭（好友靠近也会出现 F，误报太频繁），代码保留等更准的判定再开启
- 参数都在 sky-mcp-server.py 顶部常量区（_COLUMN_X_TOL / _CHAT_GAP_MIN / _CHAT_GAP_MAX / _NICKNAME_FRAMES / _F_COOLDOWN / _F_DETECT_ENABLED），随时可调

### 临时库（已返回/已发送内容）
- 窗口：5 分钟（300 秒）
- 5 分钟内，同样的文字不会再次返回给 AI
- 注意：如果朋友 5 分钟内连发两句一模一样的「好」，第二句可能不会上报（这是防重复的代价）
- 发送过的内容额外记 60 秒：读屏只要读到它的任意片段（去空格/标点后 ≥2 个字、并且是发送句子的一部分），也不再返回，避免自己发的话被拆成碎片返回

---

## 六、常见问题

1. 手机连不上？
   - 确认服务器是 0.0.0.0 启动（快捷方式已经是）；确认 token 填对；确认手机和电脑在同一 WiFi
2. 改了代码没效果？
   - 需要重启服务器，加载的是新代码
3. 老提示「发送失败」？
   - 可能是气泡飘太快、聊天框没打开、或 OCR 没读全；看黑色窗口里的日志，发给我帮你调
4. 端口被占用？
   - 说明还有旧服务器在跑，先关掉旧窗口再启动
